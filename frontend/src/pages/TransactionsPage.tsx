import { PageHeader } from '@/shared/ui/PageHeader';

export function TransactionsPage() {
  return (
    <PageHeader
      title="Transações"
      description="Extrato de liquidação com filtros, ordenação e paginação no servidor."
    />
  );
}
