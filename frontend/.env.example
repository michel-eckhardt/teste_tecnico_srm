# Backend origin used by the Vite dev/preview proxy (`/api` -> this origin). Not exposed to the bundle
# (no VITE_ prefix): the SPA always calls same-origin relative URLs (`/api/v1/...`).
API_PROXY_TARGET=http://localhost:8080
